# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# User Settings
# This is where you put your own settings as this will not be touched during update 
# if the upgrade.sh is used.

# refer to Hyprland wiki for more info https://wiki.hyprland.org/Configuring/Variables/
# NOTE: some settings are in ~/.config/hypr/UserConfigs/UserDecorAnimations.conf
#
# Look on ~/.config/hypr/configs/SystemSettings.conf to know how to modify this

# Fix DRM page-flip contention causing screen lag.
# vrr=2 (always) caused 15k+ "Device or resource busy" errors in the log.
# vrr=1 limits VRR to fullscreen apps only, which the GPU handles without stutter.
misc {
    vrr = 1
}

